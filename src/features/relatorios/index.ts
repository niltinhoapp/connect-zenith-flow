/**
 * Feature: Relatórios
 * Module key: "relatorios" (see src/config/modules.ts)
 *
 * Public surface of the Relatórios feature. Domain logic (api, hooks, schema,
 * components) is added here from F2 onward; UI screens currently live in
 * src/routes and are migrated to consume this module without visual changes.
 */
export const MODULE_KEY = "relatorios" as const;
