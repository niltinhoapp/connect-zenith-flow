/**
 * Feature: Configurações
 * Module key: "configuracoes" (see src/config/modules.ts)
 *
 * Public surface of the Configurações feature. Domain logic (api, hooks, schema,
 * components) is added here from F1 onward; UI screens currently live in
 * src/routes and are migrated to consume this module without visual changes.
 */
export const MODULE_KEY = "configuracoes" as const;
